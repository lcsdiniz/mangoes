import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  pagesContainer: {
    margin: '64px 0',
    display: 'flex',
    flexDirection: 'column',
    gap: '16px',
  },

  link: {
    textDecoration: 'none',
  },
}));
