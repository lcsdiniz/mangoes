import { createStyles } from '@mantine/core';

export const useStyles = createStyles((theme, _params, getRef) => ({
  mangaListSection: {
    padding: '16px 64px'
  },

  sectionTitle: {
    fontSize: 32,
    color: 'gray',
    margin: 0,
    lineHeight: 'auto'
  }
}))